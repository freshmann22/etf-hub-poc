# Dependencies
node_modules/

# Build and coverage
dist/
build/
coverage/

# Test artifacts
playwright-report/
test-results/
blob-report/
.nyc_output/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment and secrets
.env
.env.*
!.env.example

# OS and editors
.DS_Store
Thumbs.db
.vscode/
.idea/

# Temporary files
*.tmp
*.temp
.cache/

# Review archive (large, not needed in source)
etf-hub-main-poc-review.zip

# Spike secrets (session cookies/tokens — never commit)
spikes/krx-direct/config.json
spikes/**/*.session.json

# ETF 메타데이터 파이프라인 raw 캐시(재생성 가능 — scripts/collect-*.mjs 로 재수집)
data/raw/

# Playwright MCP 스냅샷/스크린샷(재생성 가능, 세션 산출물)
.playwright-mcp/
# 화면 검증 스크린샷(루트 임시 산출물)
*-390.png
ds-align-*.png
datasupply-*.png
toss-*.png
full-universe-*.png
