# MULTI_AGENT_WORKFLOW

Fable(총괄) + Claude Sonnet(구현) + Codex(독립 검증) 멀티에이전트 개발 워크플로.
이 문서는 ETF 비교 웹페이지 PoC(2026-07-10)에서 실제로 완주한 절차를
프로젝트 종류에 종속되지 않게 일반화한 실행 규칙이며, 이후 범용 Skill이
이 문서를 그대로 실행 규칙으로 사용할 수 있도록 작성되었다.

용어:
- **총괄(Fable)** — 작업 분해·배정·판정·통합을 수행하는 오케스트레이터.
- **구현 에이전트(Claude Sonnet)** — 제품 코드를 작성하는 서브에이전트.
- **검증 에이전트(Codex)** — MCP로 호출되는 독립 테스트·리뷰 에이전트.
- **계약 문서** — 구현과 테스트가 공통으로 따르는 인터페이스 명세
  (스키마, 함수 시그니처, 엣지케이스, 에러 문구, 모듈 방식).

---

## 1. 전체 실행 순서

| # | 단계 | 담당 | 병렬성 |
|---|---|---|---|
| S1 | 프로젝트 목표 확인 | Fable | — |
| S2 | 인터페이스 계약 작성 | Fable | — |
| S3 | 구현 | Claude Sonnet | S4와 병렬 |
| S4 | 테스트 작성 | Codex | S3와 병렬 |
| S5 | 독립 리뷰 | Codex | S3 완료 후 |
| S6 | 테스트 실행 | Fable | S3·S4 완료 후 |
| S7 | 실패 원인 판정 | Fable | 실패 시에만 |
| S8 | 원 담당자 부분 재작업 | Claude 또는 Codex | 판정 결과에 따라 |
| S9 | 화면(동작) 검증 | Fable | S6 전체 통과 후 |
| S10 | 최종 평가·보고 | Fable → 사용자 | — |

S6→S7→S8은 전체 테스트가 통과할 때까지 반복하는 루프다.
S5의 지적사항 처리도 S7의 판정 절차를 그대로 따른다.

## 2. 단계별 상세 — 담당자, 입력, 결과물, 통과 조건

### S1. 프로젝트 목표 확인 — 담당: Fable

- **입력**: 프로젝트 브리프(목표·최소 기능·제외 범위·완료 조건을 담은 문서)와
  역할 정의 문서. 없으면 Fable이 사용자에게 요구사항을 받아 먼저 작성한다.
- **결과물**: 작업 분해(단계·담당·선행조건·완료조건·검증 방법)를 담은 실행 계획.
- **통과 조건**: **사용자가 실행 계획을 승인**한다. (필수 승인 지점 §7-1)

### S2. 인터페이스 계약 작성 — 담당: Fable

- **입력**: 승인된 실행 계획, 프로젝트 브리프.
- **결과물**: 계약 문서 파일. 최소 포함 항목:
  1. 데이터 스키마 (식별자 포함, 필드별 자료형)
  2. 핵심 함수/API별: 입력, 반환, 부작용(원본 변경) 여부, 정상 사례,
     엣지케이스 처리 방식, **에러 타입과 에러 메시지 문구까지 확정**
  3. 모듈/실행 방식 (구현과 테스트가 같은 코드를 로드할 수 있는 방법)
  4. 에이전트별 담당 파일 목록 (겹치지 않게 — §5 파일 충돌 참조)
  5. 최소 기능과 계약 요소의 1:1 대응표
- **통과 조건**: 브리프의 최소 기능 전부가 계약 요소에 대응되고,
  구현 파일 집합과 테스트 파일 집합의 교집합이 공집합이다.
- **핵심 원칙**: 검증 에이전트는 구현을 보지 않고 **이 문서만으로** 테스트를
  작성하므로, 계약이 모호하면 이후 전 단계가 흔들린다. 에러 문구·기본값·
  엣지케이스를 "문자 그대로 따를 수 있는" 수준까지 확정할 것.

### S3. 구현 — 담당: Claude Sonnet (서브에이전트, 백그라운드 실행)

- **입력**: 계약 문서. (프로젝트 브리프 요약을 프롬프트에 포함)
- **결과물**: 계약이 지정한 구현 파일들 + 보고(생성 파일 목록, 구현 내용,
  남은 문제·계약 모호점).
- **작업 규칙**: 배정된 파일만 작성. 테스트 파일 접근 금지. 외부 의존성
  추가 금지(계약이 허용한 경우 제외). 계약과 충돌하면 임의 변경하지 말고
  보고. 테스트 실행 금지(실행은 Fable 권한).
- **통과 조건**: 배정 파일이 모두 존재하고, Fable이 계약의 시그니처·파일
  경계 준수를 확인한다.

### S4. 테스트 작성 — 담당: Codex (MCP, workspace-write 샌드박스)

- **입력**: **계약 문서만.** 구현 파일은 읽기·수정 모두 금지.
- **결과물**: 테스트 파일(들) + 보고(테스트 범위, 테스트 사각지대).
- **작업 규칙**: 배정된 테스트 파일만 작성. 픽스처는 자체 정의(구현의
  데이터 파일 import 금지). 계약의 정상 사례·엣지케이스·불변성(부작용
  없음)을 모두 커버. 테스트 실행 금지. 외부 패키지 금지.
- **통과 조건**: 계약의 함수/엣지케이스별 테스트가 존재하고, Fable이
  파일 경계 준수를 확인한다. (S3과 병렬 — 서로를 기다리지 않는다)

### S5. 독립 리뷰 — 담당: Codex (MCP, **read-only 샌드박스 필수**)

- **입력**: 프로젝트 브리프, 계약 문서, 구현 파일 전체. 테스트 파일은 제외.
- **결과물**: 3분류 보고 — ① 확인된 문제 ② 잠재적 위험 ③ 문제 없음.
  각 지적에 근거 파일과 위반 조항을 명시.
- **통과 조건**: 브리프의 최소 기능·제외 범위 각각에 대해 판정이 존재한다.
- **주의**: 리뷰 결과를 그대로 수용하지 않는다. 처리 방식은 §5 "리뷰 오탐" 참조.

### S6. 테스트 실행 — 담당: Fable (직접 실행, 에이전트 호출 없음)

- **입력**: S3의 구현 + S4의 테스트.
- **결과물**: 실행 결과 원문(전체/통과/실패 수, 실패 테스트명, 오류 메시지).
- **통과 조건**: 전체 통과 → S9로. 실패 존재 → S7로.

### S7. 실패 원인 판정 — 담당: Fable

- **입력**: 실패 출력 원문, 계약 문서, 해당 구현·테스트 코드.
- **판정 절차**: 실패 출력의 실제값(actual)과 계약 문구를 대조한다.
  - 구현이 계약과 다르게 동작 → **구현 오류** (Claude 재작업)
  - 구현은 계약대로인데 테스트의 기대·작성 방식이 틀림 → **테스트 오류**
    (Codex 재작업)
  - 계약 자체가 모호·모순 → **계약 오류** (Fable이 계약을 수정하고 영향
    받는 쪽에 재작업 배정)
- **결과물**: 실패 건별 귀속 판정과 재작업 범위(파일·함수·assertion 단위).
- **통과 조건**: 모든 실패에 귀속 판정이 붙는다. "실패 = 구현 문제"로
  자동 귀속하지 않는다 (PoC에서 실패 4건 전부가 테스트 결함이었음).

### S8. 원 담당자 부분 재작업 — 담당: 판정된 원 담당 에이전트

- **입력**: 실패 건별 판정, **정확히 한정된 수정 범위**.
- **작업 규칙**: 지정된 실패분만 수정. 통과 중인 코드·테스트 수정 금지.
  새 기능·새 테스트 추가 금지. 상대 에이전트의 파일 접근 금지.
- **결과물**: 수정 내역 보고(변경 전/후, 변경 이유).
- **통과 조건**: Fable이 변경이 지정 범위 안인지 확인 → S6 재실행.
  전체 재시작은 금지. 해결될 때까지 S6→S7→S8 루프.

### S9. 화면(동작) 검증 — 담당: Fable (Playwright 등 브라우저/실행 도구)

- **입력**: 통합된 코드, 브리프의 최소 기능 목록.
- **결과물**: 기능별 통과/실패 표 + 콘솔 오류·화면 깨짐 여부.
- **검증 항목 도출 규칙**: 브리프의 최소 기능 각각 + 대표 오류 경로
  (잘못된 입력 시 안내 표시 등) + 콘솔 오류 확인. 단위 테스트가 못 보는
  영역(UI 연결, 모듈 로딩, 실제 데이터 파일)을 여기서 커버한다.
- **통과 조건**: 최소 기능 전부 통과, 치명적 콘솔 오류 없음.
  실패 발견 시 재현 절차를 기록하고 S7 판정 절차로 회부.

### S10. 최종 평가·보고 — 담당: Fable → 사용자

- **입력**: S1~S9의 전체 기록.
- **결과물**: ① 브리프 완료 조건별 판정 ② 정량 기록(§9 지표)
  ③ 잔존 위험 목록 ④ 확인된 사실/미검증 가정 구분.
- **통과 조건**: **사용자의 최종 완료 승인.** (필수 승인 지점 §7-5)

## 3. 오류 발생 시 처리 방식

| 오류 유형 | 처리 |
|---|---|
| **구현 오류** | S7 판정 후 Claude에게 해당 함수/파일 단위로만 재작업 배정. 테스트는 손대지 않음 |
| **테스트 오류** | S7 판정 후 Codex에게 해당 assertion/케이스 단위로만 재작업 배정. 구현은 손대지 않음. (PoC 실증: assertion 4개만 수정, 1회에 해결) |
| **리뷰 오탐** | Codex의 "확인된 문제"는 **Fable이 지적된 파일을 직접 읽어 재현 확인한 후에만** 재작업 배정. 재현 불가 시 근거를 남기고 기각. (PoC 실증: 오탐 2건을 이 절차로 차단 — 특히 비ASCII 텍스트 파일에 대한 지적은 오탐 가능성을 우선 의심) |
| **파일 충돌** | 사전 예방이 원칙: 계약(S2)에서 에이전트별 파일 집합을 서로소로 배정. 같은 파일 수정이 불가피하면 반드시 순차 진행(앞 에이전트 완료 후 시작). 그럼에도 충돌 발생 시 Fable이 나중 변경을 중단시키고 배정을 다시 나눔. 수동 병합은 워크플로 실패로 기록 |
| **MCP 연결 실패** | 작업 시작 전 `claude mcp list`로 연결 확인. 호출 실패 시 1회 재시도 → 계속 실패하면 사용자에게 보고하고 해당 단계를 보류(다른 병렬 작업은 계속). Codex 몫을 Fable/Claude가 대신 수행하지 않는다(독립 검증 무력화 방지) |
| **에이전트 무응답/이상 산출물** | 산출물이 파일 경계나 계약을 위반하면 수용하지 않고 같은 범위로 재지시. 2회 연속 실패 시 사용자에게 보고 |

## 4. 자동 진행 구간과 필수 승인 지점

### 자동으로 진행해도 되는 구간

S2~S9 전체: 계약 작성, 병렬 구현·테스트, 리뷰, 테스트 실행,
실패 판정, 부분 재작업 루프, 화면 검증.
(PoC에서 이 구간의 사용자 개입은 단계별 게이트 승인뿐이었고,
코드 판단이 필요한 개입은 0회였다 — 확인된 사실.
단, 실제 무인 연속 실행은 미검증 — §5 참조.)

자동 구간에서도 즉시 중단하고 사용자에게 보고해야 하는 경우:
- 추가 비용(종량 과금 등) 발생 가능성
- API Key·외부 계정 로그인 등 인증 필요
- 개인정보·비밀번호·결제정보·사내 기밀 입력 필요
- 외부 API 연결 또는 외부 서비스 사용 필요
- 배포·게시·PR 생성 등 외부 공개 필요
- 파일 삭제·git 이력 변경·대규모 덮어쓰기 등 파괴적 변경 필요
- 계약으로 해소되지 않는 요구사항 모순 발견
- 동일 오류에 대한 재작업 3회 실패
- Codex MCP 연결 실패가 1회 재시도 후에도 지속 (§3 참조)
- 구독 사용량 한도 임박 또는 모델 사용 불가

그 밖의 일반적인 구현 선택, 테스트 실패 처리, 리뷰 결과 판정, 비파괴적
파일 생성·수정, 로컬 명령 실행은 사용자에게 묻지 않고 총괄이 판단해
계속 진행한다.

### 실행 로그 (MULTI_AGENT_RUN_LOG.md)

자동 구간(S2~) 시작 시 총괄이 루트에 `MULTI_AGENT_RUN_LOG.md`를 생성하고
단계별 시작·완료, 담당 에이전트, 생성·수정 파일, 테스트 결과, 재작업 횟수,
사용자 개입 사유를 기록한다. 로그 기록은 총괄 전담이며 구현·검증
에이전트에게 배정하지 않는다.

### 반드시 사용자 승인을 받아야 하는 지점

1. **최초 실행 계획 승인** (S1 종료 시) — 이후 전체가 계획·계약에 종속됨
2. **파괴적 변경** — 기존 파일·데이터 삭제, 덮어쓰기, git 이력 변경 등
   되돌리기 어려운 작업 일체
3. **외부 API 또는 계정 사용** — 외부 서비스 호출, 로그인, 과금 유발 행위
4. **배포** — 외부에 공개되는 모든 행위 (호스팅, 패키지 배포, PR 생성 포함)
5. **최종 완료 승인** (S10) — 완료 판정과 잔존 위험 수용은 사용자 몫

## 5. 확인된 사실 vs 미검증 가정

### 이번 PoC에서 실제로 확인된 사실

- 구독 환경(Claude Max + ChatGPT Plus/Codex MCP)만으로 S1~S10 전체
  사이클이 API 종량 과금 없이 완주된다.
- 계약 문서를 먼저 확정하면 구현(S3)과 테스트 작성(S4)을 **진짜 병렬**로
  실행할 수 있고, 산출물이 충돌 없이 자동 취합된다 (수동 병합 0회).
- 파일 집합을 서로소로 배정하면 동시 수정 충돌이 구조적으로 발생하지 않는다.
- 부분 재작업이 동작한다: 실패 4건 → 원 담당자(Codex)에게 assertion 4개만
  재배정 → 1회 수정으로 19/19 통과, 기존 통과분 무손상.
- 실패의 원천이 구현이 아니라 테스트일 수 있다 (실패 4건 전부 테스트 결함).
- 검증 에이전트의 리뷰에는 오탐이 섞인다 (확인된 문제 2건 중 2건이 오탐).
  총괄의 코드 대조 판정 단계가 이를 걸러냈다.
- 교차 검증이 실제로 상호 보완적이다: Codex는 Claude 구현의 UX 이슈를,
  Fable은 Codex의 오탐을, 테스트 실행은 Codex 테스트의 결함을 각각 잡았다.
- 단위 테스트 전체 통과(19/19) 상태에서도 화면 검증이 별도로 필요하다
  (모듈 로딩·UI 연결·실제 데이터는 단위 테스트 밖 — 콘솔 오류 확인 포함).

### 아직 검증되지 않은 가정

- **무인 연속 실행**: PoC는 단계마다 사용자 게이트를 두고 진행했다.
  한 번의 지시로 S2~S9를 무중단 완주할 수 있다는 것은 미검증.
- **수동 방식 대비 개입 감소**: 동일 프로젝트를 단일 에이전트로 수행한
  대조 기록이 없어 정량 비교 불가.
- **규모 확장성**: 파일 8개 규모에서만 검증됨. 대규모 코드베이스, 강결합
  리팩터링, 파일 경계로 나눌 수 없는 작업에서의 유효성은 미검증.
- **Fable 사용량 집중**: 총괄에 토큰 소비가 집중되어 구독 한도에 먼저
  닿을 것이라는 예상은 측정되지 않은 추정.
- **Codex 오탐의 원인**: 한글 등 비ASCII 텍스트 인식 문제라는 설명은
  정황 추정이며 재현 실험으로 확인되지 않음.
- **다른 기술 스택 일반화**: 빌드 도구·프레임워크·컴파일 언어가 있는
  프로젝트에서 동일 절차가 성립하는지 미검증 (계약·경계 원칙은 스택
  독립적이지만 실증은 정적 웹앱 1건뿐).

## 6. 범용화 원칙 (Skill 구현 시 준수 사항)

이 문서를 실행 규칙으로 쓰는 Skill은 다음을 프로젝트별 입력으로 받고,
나머지 절차는 이 문서를 따른다:

1. **프로젝트 브리프** — 목표, 최소 기능 목록, 제외 범위, 완료 조건
2. **역할 매핑** — 어떤 모델/도구가 총괄·구현·검증을 맡는지
   (본 문서의 Fable/Claude/Codex는 역할명이며 특정 모델에 고정되지 않음.
   단, 구현과 검증은 **서로 다른 에이전트**여야 독립 검증이 성립한다)
3. **검증 명령** — 테스트 실행 방법(S6)과 동작 검증 방법(S9)
4. **파일 경계 규칙** — 구현 파일 집합과 검증 파일 집합의 분리 기준

불변 원칙 (프로젝트 종류와 무관하게 유지):
- 계약 먼저, 병렬은 그 다음
- 구현자와 검증자는 서로의 산출물에 쓰기 접근 불가
- 검증자는 계약만 보고 테스트를 작성 (구현 참조 금지)
- 리뷰 지적은 총괄의 재현 확인 후에만 재작업으로 전환
- 재작업은 항상 실패 단위로, 원 담당자에게
- 파괴적 변경·외부 공개·과금 행위는 사용자 승인 없이 금지
