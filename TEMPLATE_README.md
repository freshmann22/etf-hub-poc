# multi-agent-template

멀티에이전트(총괄 + 구현 + 독립 검증) 개발 워크플로를 새 프로젝트에서 바로
쓸 수 있게 만든 템플릿이다. Claude Code의 총괄 에이전트가 실행 계획과
인터페이스 계약을 만들고, Claude 서브에이전트(구현)와 Codex MCP(테스트
작성·독립 리뷰)를 병렬로 조율하며, 테스트 실행·오탐 판정·부분 재작업
루프·화면 검증까지 자동으로 진행한다.

## 포함 파일

| 파일 | 역할 |
|---|---|
| `MULTI_AGENT_WORKFLOW.md` | 실행 규칙 — 단계(S1~S10), 담당자, 통과 조건, 오류 처리, 승인 지점 |
| `.mcp.json` | Codex MCP 서버 등록 (stdio, `codex mcp-server`) |
| `.claude/skills/multi-agent-start/SKILL.md` | `/multi-agent-start` Skill — 워크플로의 원스톱 실행기 |
| `README.md` | 이 문서 |

> 이 템플릿에는 워크플로 실행에 필요한 파일만 들어 있다. 원본 PoC(Todo
> 보드)의 코드, 테스트, 프로젝트 브리프, 계약 문서, 실행 로그는 포함하지
> 않는다 — 그것들은 프로젝트별 산출물이며 실행 중에 새로 만들어진다.

## 사전 조건

1. **Claude Code** 설치 (구현·총괄 에이전트)
2. **Codex CLI** 설치 (`codex` 명령이 PATH에 있어야 함 — 독립 검증 에이전트)
3. 두 도구 모두 **계정 로그인 완료** 상태 (Claude 계정, ChatGPT/Codex 계정)

## 새 프로젝트에 적용하는 방법

1. 이 폴더의 내용 전체를 새 프로젝트 루트로 복사한다
   (`.claude/`, `.mcp.json` 같은 숨김 항목 포함):

   ```powershell
   Copy-Item -Recurse C:\Users\tleod\projects\multi-agent-template\* <새-프로젝트-경로>\
   Copy-Item -Recurse C:\Users\tleod\projects\multi-agent-template\.claude <새-프로젝트-경로>\
   Copy-Item C:\Users\tleod\projects\multi-agent-template\.mcp.json <새-프로젝트-경로>\
   ```

2. 새 프로젝트 폴더에서 git 저장소를 준비한다 (`git init` — Skill이
   Git 저장소를 전제한다).

## Claude Code 실행과 Codex MCP 최초 승인

1. 새 프로젝트 폴더에서 `claude`를 실행한다.
2. 프로젝트에 `.mcp.json`이 있으면 Claude Code가 **프로젝트 MCP 서버
   사용 여부를 묻는다** — codex 서버를 승인한다 (최초 1회).
3. 연결 확인: `claude mcp list` 실행 시 `codex: ... ✔ Connected`가
   보여야 한다. 연결되지 않으면 Skill이 시작 단계에서 멈춘다.

## 실행 방법

Claude Code 프롬프트에서:

```
/multi-agent-start
```

Skill이 사전 확인(Git 저장소, Codex 연결, 브리프 존재)을 거친 뒤,
브리프가 없으면 아래 5가지를 질문한다.

### 사용자가 처음 제공할 브리프 항목

1. **무엇을 만들 것인지** — 한 문단 이내의 목표
2. **최소 기능** — 번호를 붙인 기능 목록 (각 기능은 검증 가능한 문장으로)
3. **제외 범위** — 하지 않을 것의 명시적 목록
4. **완료 조건** — 무엇이 충족되면 끝났다고 판정하는지
5. **검증 방법** — 자동 테스트 실행 방법과 동작(화면) 확인 방법

## 계획 승인 후에는 자동으로 진행된다

실행 계획을 사용자가 승인하면(필수 정지 지점 1), 이후 **계약 작성 →
구현·테스트 병렬 → 독립 리뷰 → 오탐 판정 → 테스트 실행 → 실패 판정 →
부분 재작업 루프 → 화면 검증**(S2~S9)이 사용자 개입 없이 자동으로
진행된다. 끝나면 최종 보고서가 출력되고 사용자 최종 승인 대기 상태에서
멈춘다(필수 정지 지점 2). 전 과정은 `MULTI_AGENT_RUN_LOG.md`에 기록된다.

## 자동 실행이 중단되는 조건

다음 중 하나라도 발생하면 자동 실행을 멈추고 사용자에게 묻는다:

1. 추가 비용(종량 과금 등) 발생 가능성
2. API Key·외부 계정 로그인 등 인증 필요
3. 개인정보·비밀번호·결제정보·사내 기밀 입력 필요
4. 외부 API 연결 또는 외부 서비스 사용 필요
5. 배포·게시·PR 생성 등 외부 공개 필요
6. 파일 삭제·git 이력 변경·대규모 덮어쓰기 등 파괴적 변경 필요
7. 요구사항 모순으로 계약을 확정할 수 없음
8. 동일 오류에 대한 재작업 3회 실패
9. Codex MCP 연결 실패가 1회 재시도 후에도 지속
10. 구독 사용량 한도 임박 또는 모델 사용 불가

그 밖의 일반적인 구현 선택, 테스트 실패 처리, 리뷰 판정, 비파괴적 파일
생성·수정, 로컬 명령 실행은 총괄이 판단해 계속 진행한다.
